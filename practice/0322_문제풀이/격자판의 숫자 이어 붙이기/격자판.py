# 격자판 -> 2차원 배열
# 방향배열 -> 총 6번 이동
# 특정 시점에서 나올 수 있는 결과가 "두 가지" -> 너 재귀니?

import sys
sys.stdin = open('input.txt')

dx
def dfs(y, x, cnt):
    # 기저조건: 7자라면 끝
    if cnt == 7:
        # 현재까지의 경로를 저장
        result.add(path)
        return
    for i in range(4):

T = int(input())
for tc in range(1, T+1):
    maps = [list(map(int, input().split())) for _ in range(4)]]
    re